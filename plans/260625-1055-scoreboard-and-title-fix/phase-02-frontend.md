# Phase 2: Frontend - Implement scoreboard overlay & padding fix

## Tasks

1. Update `DisplayPage.tsx`:
   - Adjust `py-4 tracking-normal leading-normal` on `<h1>` tags to fix the Vietnamese accent clipping issues.
   - Implement the `ScoreboardOverlay` component and render it at the bottom.
2. Update `AdminPage.tsx`:
   - Add a button `📊 BẢNG ĐIỂM CHIẾU` in the header panel next to `📺 BẬT INTRO`.
   - Add a toggle button on the header of the "Bảng Điểm Nhanh" sidebar block.
